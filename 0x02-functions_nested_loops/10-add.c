#include "main.h"

/**
 * add - Adds two integers and returns the result;
 * @n: First integer;
 * @m: Second integer;
 * Return: Sum of the integers.
 */
int add(int n, int m)
{
	int s;

	s = n + m;
	return (s);
}
